== Welcome to The Facebook On Rails Before App ==

To install just run the following steps:

git clone https://github.com/facebookrails/facebook_on_rails_before.git

bundle install

rake db:migrate

To start the server just run:

rails server